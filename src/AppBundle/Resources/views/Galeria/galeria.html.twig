{% extends ':default:montujemymeble.html.twig' %}

{% block bodyblock %}
    <div class="row" id="naglowek">
        <h3>Galeria</h3>
    </div>
    <div class="row" id="bodyblock">
        {% for photo in view %}
            <div id="galeria">
                <a target="_blank" href="{{ photo.url_m }}">
                    <img src="{{ photo.url_q }}">
                </a>
            </div>
        {% endfor %}
    </div>
{% endblock %}
